<Window x:Class="PsMidiProfiler.ProfileActivator"
        x:Name="this"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ProfileActivator" Height="300" Width="300">
    <Grid>
        <Label Content="Choose your user name:" />
        <ComboBox ItemsSource="{Binding ElementName=this, Path=PsUserProfiles}"
                  SelectedItem="{Binding ElementName=this, Path=CurrentPsUserProfile}"
                  IsSynchronizedWithCurrentItem="True"
                  Height="25" Width="150" HorizontalAlignment="Left" VerticalAlignment="Top"
                  Margin="0,30,0,0" />
        <Button Content="Activate MIDI Profile" Width="150" Height="25"
                Command="{Binding ElementName=this, Path=ActivateCommand}"/>
    </Grid>
</Window>
